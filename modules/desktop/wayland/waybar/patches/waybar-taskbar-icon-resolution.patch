diff --git a/include/modules/wlr/taskbar.hpp b/include/modules/wlr/taskbar.hpp
--- a/include/modules/wlr/taskbar.hpp
+++ b/include/modules/wlr/taskbar.hpp
@@ -81,0 +82 @@ class Task {
+  std::string initial_app_id_;
@@ -93,0 +95 @@ class Task {
+  void resolve_app_info();
diff --git a/include/util/icon_loader.hpp b/include/util/icon_loader.hpp
--- a/include/util/icon_loader.hpp
+++ b/include/util/icon_loader.hpp
@@ -21 +21 @@ class IconLoader {
-  static Glib::RefPtr<Gio::DesktopAppInfo> get_desktop_app_info(const std::string& app_id);
+  static Glib::RefPtr<Gio::DesktopAppInfo> get_desktop_app_info(const std::string& candidate);
@@ -26 +26,2 @@ class IconLoader {
-                              Glib::RefPtr<Gio::DesktopAppInfo> app_info, int size);
+                               Glib::RefPtr<Gio::DesktopAppInfo> app_info,
+                               const std::vector<std::string>& fallback_icon_names, int size);
@@ -31,0 +33,8 @@ class IconLoader {
+  bool image_load_icon(Gtk::Image& image, Glib::RefPtr<Gio::DesktopAppInfo> app_info,
+                       const std::vector<std::string>& fallback_icon_names, int size) const;
+  static std::vector<std::string> get_icon_candidates(const std::string& app_id,
+                                                       const std::string& title,
+                                                       bool use_title);
+  static bool should_use_title(const std::string& app_id, const std::string& title);
+  static Glib::RefPtr<Gio::DesktopAppInfo> get_app_info_from_candidates(
+      const std::vector<std::string>& candidates);
diff --git a/src/modules/wlr/taskbar.cpp b/src/modules/wlr/taskbar.cpp
--- a/src/modules/wlr/taskbar.cpp
+++ b/src/modules/wlr/taskbar.cpp
@@ -201 +201,2 @@ void Task::handle_title(const char* title) {
-  if (!with_icon_ && !with_name_ || app_info_) {
+  if ((!with_icon_ && !with_name_) ||
+      !IconLoader::should_use_title(initial_app_id_, title_)) {
@@ -205,12 +206 @@ void Task::handle_title(const char* title) {
-  app_info_ = IconLoader::get_app_info_from_app_id_list(title_);
-  name_ = app_info_ ? app_info_->get_display_name() : title;
-
-  if (!with_icon_) {
-    return;
-  }
-
-  int icon_size = config_["icon-size"].isInt() ? config_["icon-size"].asInt() : 16;
-  if (tbar_->icon_loader().image_load_icon(icon_, app_info_, icon_size))
-    icon_.show();
-  else
-    spdlog::debug("Couldn't find icon for {}", title_);
+  resolve_app_info();
@@ -247 +237,2 @@ void Task::handle_app_id(const char* app_id) {
-  app_id_ = app_id;
+  initial_app_id_ = app_id;
+  app_id_ = initial_app_id_;
@@ -262,2 +253,12 @@ void Task::handle_app_id(const char* app_id) {
-  app_info_ = IconLoader::get_app_info_from_app_id_list(app_id_);
-  name_ = app_info_ ? app_info_->get_display_name() : app_id;
+  resolve_app_info();
+}
+
+void Task::resolve_app_info() {
+  if (app_id_ == "unknown" && title_.empty()) {
+    return;
+  }
+
+  const bool use_title = IconLoader::should_use_title(initial_app_id_, title_);
+  const auto candidates = IconLoader::get_icon_candidates(app_id_, title_, use_title);
+  app_info_ = IconLoader::get_app_info_from_candidates(candidates);
+  name_ = app_info_ ? app_info_->get_display_name() : app_id_;
@@ -270 +267 @@ void Task::handle_app_id(const char* app_id) {
-  if (tbar_->icon_loader().image_load_icon(icon_, app_info_, icon_size))
+  if (tbar_->icon_loader().image_load_icon(icon_, app_info_, candidates, icon_size))
@@ -273 +270 @@ void Task::handle_app_id(const char* app_id) {
-    spdlog::debug("Couldn't find icon for {}", app_id_);
+    spdlog::debug("Couldn't find icon for {}", initial_app_id_);
diff --git a/src/util/icon_loader.cpp b/src/util/icon_loader.cpp
--- a/src/util/icon_loader.cpp
+++ b/src/util/icon_loader.cpp
@@ -4,0 +5,126 @@
+#include <algorithm>
+#include <array>
+#include <cctype>
+#include <ranges>
+#include <sstream>
+
+namespace {
+
+std::string trim_copy(const std::string& value) {
+  const auto first = value.find_first_not_of(" \t\n\r");
+  if (first == std::string::npos) return "";
+  return value.substr(first, value.find_last_not_of(" \t\n\r") - first + 1);
+}
+
+std::string lower_copy(std::string value) {
+  std::ranges::transform(value, value.begin(), [](unsigned char c) { return std::tolower(c); });
+  return value;
+}
+
+std::string normalize_identity(const std::string& value) {
+  std::string normalized;
+  for (const unsigned char character : lower_copy(value)) {
+    if (std::isalnum(character)) normalized += character;
+  }
+  return normalized;
+}
+
+void add_candidate(std::vector<std::string>& candidates, const std::string& candidate) {
+  const auto trimmed = trim_copy(candidate);
+  if (trimmed.empty() || std::ranges::find(candidates, trimmed) != candidates.end()) return;
+  candidates.push_back(trimmed);
+}
+
+void add_value_candidates(std::vector<std::string>& candidates, const std::string& value) {
+  const auto trimmed = trim_copy(value);
+  if (trimmed.empty()) return;
+
+  add_candidate(candidates, trimmed);
+  const auto lowered = lower_copy(trimmed);
+  add_candidate(candidates, lowered);
+  add_candidate(candidates, normalize_identity(trimmed));
+
+  auto normalized_separators = lowered;
+  std::ranges::replace(normalized_separators, '_', '-');
+  std::ranges::replace(normalized_separators, ' ', '-');
+  add_candidate(candidates, normalized_separators);
+
+  std::string hyphenated;
+  for (size_t index = 0; index < trimmed.size(); index++) {
+    const auto character = static_cast<unsigned char>(trimmed[index]);
+    if (std::isupper(character) && index > 0 && std::islower(static_cast<unsigned char>(trimmed[index - 1]))) {
+      hyphenated += '-';
+    }
+    hyphenated += static_cast<char>(std::tolower(character));
+  }
+  add_candidate(candidates, hyphenated);
+  std::ranges::replace(hyphenated, '-', '_');
+  add_candidate(candidates, hyphenated);
+}
+
+std::string without_trailing_parentheticals(std::string value) {
+  while (!value.empty() && value.back() == ')') {
+    const auto start = value.rfind('(');
+    if (start == std::string::npos || start == 0 || value[start - 1] != ' ') break;
+    value = trim_copy(value.substr(0, start));
+  }
+  return value;
+}
+
+std::string without_trailing_version(const std::string& value) {
+  const auto separator = value.rfind(' ');
+  if (separator == std::string::npos) return value;
+
+  const auto version = value.substr(separator + 1);
+  if (version.find('.') == std::string::npos ||
+      !std::ranges::all_of(version, [](unsigned char character) {
+        return std::isdigit(character) || character == '.';
+      })) {
+    return value;
+  }
+  return trim_copy(value.substr(0, separator));
+}
+
+std::vector<std::string> title_candidates(const std::string& title) {
+  auto value = trim_copy(title);
+  constexpr std::string_view grabbed = " (grabbed)";
+  if (value.size() >= grabbed.size() &&
+      lower_copy(value.substr(value.size() - grabbed.size())) == grabbed) {
+    value.resize(value.size() - grabbed.size());
+  }
+  value = trim_copy(value);
+
+  std::vector<std::string> candidates;
+  add_value_candidates(candidates, value);
+  add_value_candidates(candidates, without_trailing_parentheticals(value));
+  add_value_candidates(candidates, without_trailing_version(value));
+
+  for (const auto delimiter : {" - ", ": "}) {
+    const auto position = value.find(delimiter);
+    if (position != std::string::npos) add_value_candidates(candidates, value.substr(0, position));
+  }
+  return candidates;
+}
+
+bool is_generic_wrapper(const std::string& app_id) {
+  static constexpr std::array<std::string_view, 8> wrapper_classes = {
+      "gamescope", "steam", "wine", "lutris", "heroic", "bottles", "umu", "proton"};
+  const auto normalized = lower_copy(app_id);
+  if (normalized.starts_with("steam_app_")) return true;
+  return std::ranges::find(wrapper_classes, normalized) != wrapper_classes.end();
+}
+
+bool matches_candidate(const Glib::RefPtr<Gio::DesktopAppInfo>& app_info,
+                       const std::string& candidate) {
+  const auto normalized_candidate = normalize_identity(candidate);
+  if (normalized_candidate.empty()) return false;
+
+  const auto matches = [&normalized_candidate](const Glib::ustring& value) {
+    return normalize_identity(value) == normalized_candidate;
+  };
+  return matches(app_info->get_id()) || matches(app_info->get_startup_wm_class()) ||
+         matches(app_info->get_display_name()) || matches(app_info->get_executable());
+}
+
+}  // namespace
+
@@ -8,2 +134,3 @@ std::vector<std::string> IconLoader::search_prefix() {
-  std::string home_dir = std::getenv("HOME");
-  prefixes.push_back(home_dir + "/.local/share/");
+  if (const char* home_dir = std::getenv("HOME")) {
+    prefixes.push_back(std::string(home_dir) + "/.local/share/");
+  }
@@ -59,2 +186,2 @@ Glib::RefPtr<Gio::DesktopAppInfo> IconLoader::get_app_info_by_name(const std::st
-Glib::RefPtr<Gio::DesktopAppInfo> IconLoader::get_desktop_app_info(const std::string& app_id) {
-  auto app_info = get_app_info_by_name(app_id);
+Glib::RefPtr<Gio::DesktopAppInfo> IconLoader::get_desktop_app_info(const std::string& candidate) {
+  auto app_info = get_app_info_by_name(candidate);
@@ -65,12 +192,5 @@ Glib::RefPtr<Gio::DesktopAppInfo> IconLoader::get_desktop_app_info(const std::st
-  std::string desktop_file = "";
-
-  gchar*** desktop_list = g_desktop_app_info_search(app_id.c_str());
-  if (desktop_list != nullptr && desktop_list[0] != nullptr) {
-    for (size_t i = 0; desktop_list[0][i]; i++) {
-      if (desktop_file == "") {
-        desktop_file = desktop_list[0][i];
-      } else {
-        auto tmp_info = Gio::DesktopAppInfo::create(desktop_list[0][i]);
-        if (!tmp_info)
-          // see https://github.com/Alexays/Waybar/issues/1446
-          continue;
+  const auto desktop_id = candidate.ends_with(".desktop") ? candidate : candidate + ".desktop";
+  app_info = Gio::DesktopAppInfo::create(desktop_id);
+  if (app_info) {
+    return app_info;
+  }
@@ -78,6 +198,4 @@ Glib::RefPtr<Gio::DesktopAppInfo> IconLoader::get_desktop_app_info(const std::st
-        auto startup_class = tmp_info->get_startup_wm_class();
-        if (startup_class == app_id) {
-          desktop_file = desktop_list[0][i];
-          break;
-        }
-      }
+  for (const auto& info : Gio::AppInfo::get_all()) {
+    auto desktop_app_info = Glib::RefPtr<Gio::DesktopAppInfo>::cast_dynamic(info);
+    if (desktop_app_info && matches_candidate(desktop_app_info, candidate)) {
+      return desktop_app_info;
@@ -85 +202,0 @@ Glib::RefPtr<Gio::DesktopAppInfo> IconLoader::get_desktop_app_info(const std::st
-    g_strfreev(desktop_list[0]);
@@ -87 +203,0 @@ Glib::RefPtr<Gio::DesktopAppInfo> IconLoader::get_desktop_app_info(const std::st
-  g_free(desktop_list);
@@ -89 +205 @@ Glib::RefPtr<Gio::DesktopAppInfo> IconLoader::get_desktop_app_info(const std::st
-  return get_app_info_by_name(desktop_file);
+  return {};
@@ -109 +225,2 @@ bool IconLoader::image_load_icon(Gtk::Image& image, const Glib::RefPtr<Gtk::Icon
-                                 Glib::RefPtr<Gio::DesktopAppInfo> app_info, int size) {
+                                 Glib::RefPtr<Gio::DesktopAppInfo> app_info,
+                                 const std::vector<std::string>& fallback_icon_names, int size) {
@@ -131 +248,5 @@ bool IconLoader::image_load_icon(Gtk::Image& image, const Glib::RefPtr<Gtk::Icon
-    } else {
+    }
+  }
+
+  if (!pixbuf) {
+    for (const auto& icon_name : fallback_icon_names) {
@@ -133,2 +254 @@ bool IconLoader::image_load_icon(Gtk::Image& image, const Glib::RefPtr<Gtk::Icon
-        pixbuf = DefaultGtkIconThemeWrapper::load_icon(
-            "image-missing", scaled_icon_size, Gtk::IconLookupFlags::ICON_LOOKUP_FORCE_SIZE);
+        pixbuf = icon_theme->load_icon(icon_name, scaled_icon_size, Gtk::ICON_LOOKUP_FORCE_SIZE);
@@ -136 +256,3 @@ bool IconLoader::image_load_icon(Gtk::Image& image, const Glib::RefPtr<Gtk::Icon
-        pixbuf = {};
+        if (Glib::file_test(icon_name, Glib::FILE_TEST_EXISTS)) {
+          pixbuf = load_icon_from_file(icon_name, scaled_icon_size);
+        }
@@ -137,0 +260,10 @@ bool IconLoader::image_load_icon(Gtk::Image& image, const Glib::RefPtr<Gtk::Icon
+      if (pixbuf) break;
+    }
+  }
+
+  if (!pixbuf) {
+    try {
+      pixbuf = DefaultGtkIconThemeWrapper::load_icon(
+          "image-missing", scaled_icon_size, Gtk::IconLookupFlags::ICON_LOOKUP_FORCE_SIZE);
+    } catch (...) {
+      pixbuf = {};
@@ -163,0 +296,5 @@ bool IconLoader::image_load_icon(Gtk::Image& image, Glib::RefPtr<Gio::DesktopApp
+  return image_load_icon(image, app_info, {}, size);
+}
+
+bool IconLoader::image_load_icon(Gtk::Image& image, Glib::RefPtr<Gio::DesktopAppInfo> app_info,
+                                 const std::vector<std::string>& fallback_icon_names, int size) const {
@@ -165 +302 @@ bool IconLoader::image_load_icon(Gtk::Image& image, Glib::RefPtr<Gio::DesktopApp
-    if (image_load_icon(image, icon_theme, app_info, size)) {
+    if (image_load_icon(image, icon_theme, app_info, fallback_icon_names, size)) {
@@ -169 +306,31 @@ bool IconLoader::image_load_icon(Gtk::Image& image, Glib::RefPtr<Gio::DesktopApp
-  return image_load_icon(image, default_icon_theme_, app_info, size);
+  return image_load_icon(image, default_icon_theme_, app_info, fallback_icon_names, size);
+}
+
+std::vector<std::string> IconLoader::get_icon_candidates(const std::string& app_id,
+                                                          const std::string& title,
+                                                          bool use_title) {
+  std::vector<std::string> candidates;
+  if (use_title) {
+    for (const auto& candidate : title_candidates(title)) add_candidate(candidates, candidate);
+  }
+
+  std::istringstream app_ids(app_id);
+  for (std::string id; app_ids >> id;) {
+    add_value_candidates(candidates, id);
+    const auto final_dot = id.rfind('.');
+    if (final_dot != std::string::npos && id.find('/') == std::string::npos) {
+      add_value_candidates(candidates, id.substr(final_dot + 1));
+    }
+  }
+  return candidates;
+}
+
+bool IconLoader::should_use_title(const std::string& app_id, const std::string& title) {
+  return is_generic_wrapper(app_id) || lower_copy(title).ends_with(" (grabbed)") || app_id == "unknown";
+}
+
+Glib::RefPtr<Gio::DesktopAppInfo> IconLoader::get_app_info_from_candidates(
+    const std::vector<std::string>& candidates) {
+  for (const auto& candidate : candidates) {
+    if (auto app_info = get_desktop_app_info(candidate)) return app_info;
+  }
+  return {};
@@ -174,33 +341 @@ Glib::RefPtr<Gio::DesktopAppInfo> IconLoader::get_app_info_from_app_id_list(
-  std::string app_id;
-  std::istringstream stream(app_id_list);
-  Glib::RefPtr<Gio::DesktopAppInfo> app_info_;
-
-  /* Wayfire sends a list of app-id's in space separated format, other compositors
-   * send a single app-id, but in any case this works fine */
-  while (stream >> app_id) {
-    app_info_ = get_desktop_app_info(app_id);
-    if (app_info_) {
-      return app_info_;
-    }
-
-    auto lower_app_id = app_id;
-    std::ranges::transform(lower_app_id, lower_app_id.begin(),
-                           [](char c) { return std::tolower(c); });
-    app_info_ = get_desktop_app_info(lower_app_id);
-    if (app_info_) {
-      return app_info_;
-    }
-
-    size_t start = 0, end = app_id.size();
-    start = app_id.rfind(".", end);
-    std::string app_name = app_id.substr(start + 1, app_id.size());
-    app_info_ = get_desktop_app_info(app_name);
-    if (app_info_) {
-      return app_info_;
-    }
-
-    start = app_id.find("-");
-    app_name = app_id.substr(0, start);
-    app_info_ = get_desktop_app_info(app_name);
-  }
-  return app_info_;
+  return get_app_info_from_candidates(get_icon_candidates(app_id_list, "", false));
diff --git a/src/util/icon_loader.cpp b/src/util/icon_loader.cpp
--- a/src/util/icon_loader.cpp
+++ b/src/util/icon_loader.cpp
@@ -187,5 +187,0 @@ Glib::RefPtr<Gio::DesktopAppInfo> IconLoader::get_desktop_app_info(const std::st
-  auto app_info = get_app_info_by_name(candidate);
-  if (app_info) {
-    return app_info;
-  }
-
@@ -193 +188 @@ Glib::RefPtr<Gio::DesktopAppInfo> IconLoader::get_desktop_app_info(const std::st
-  app_info = Gio::DesktopAppInfo::create(desktop_id);
+  auto app_info = Gio::DesktopAppInfo::create(desktop_id);
diff --git a/src/util/icon_loader.cpp b/src/util/icon_loader.cpp
--- a/src/util/icon_loader.cpp
+++ b/src/util/icon_loader.cpp
@@ -193,2 +193,8 @@ Glib::RefPtr<Gio::DesktopAppInfo> IconLoader::get_desktop_app_info(const std::st
-  for (const auto& info : Gio::AppInfo::get_all()) {
-    auto desktop_app_info = Glib::RefPtr<Gio::DesktopAppInfo>::cast_dynamic(info);
+  gchar*** desktop_list = g_desktop_app_info_search(candidate.c_str());
+  if (desktop_list == nullptr || desktop_list[0] == nullptr) {
+    g_free(desktop_list);
+    return {};
+  }
+
+  for (size_t index = 0; desktop_list[0][index]; index++) {
+    auto desktop_app_info = Gio::DesktopAppInfo::create(desktop_list[0][index]);
@@ -195,0 +202,2 @@ Glib::RefPtr<Gio::DesktopAppInfo> IconLoader::get_desktop_app_info(const std::st
+      g_strfreev(desktop_list[0]);
+      g_free(desktop_list);
@@ -199,0 +208,2 @@ Glib::RefPtr<Gio::DesktopAppInfo> IconLoader::get_desktop_app_info(const std::st
+  g_strfreev(desktop_list[0]);
+  g_free(desktop_list);
diff --git a/src/util/icon_loader.cpp b/src/util/icon_loader.cpp
--- a/src/util/icon_loader.cpp
+++ b/src/util/icon_loader.cpp
@@ -341,0 +342 @@ Glib::RefPtr<Gio::DesktopAppInfo> IconLoader::get_app_info_from_candidates(
+  return {};
