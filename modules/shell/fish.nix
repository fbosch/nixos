{
  flake.modules.nixos.shell =
    { pkgs, ... }:
    {
      environment.shells = [
        pkgs.fish
        pkgs.dash
      ];
    };

  flake.modules.homeManager.shell =
    { config
    , lib
    , pkgs
    , ...
    }:
    let
      secretNamesByEnv = {
        GITHUB_TOKEN = "github-token";
        COPILOT_TOKEN = "github-token";
        KAGI_API_TOKEN = "kagi-api-token";
        OPENAI_API_KEY = "openai-api-key";
        EXA_API_KEY = "exa-api-key";
      };

      availableSecretNamesByEnv = lib.filterAttrs
        (
          _: secretName: lib.hasAttrByPath [ "sops" "secrets" secretName "path" ] config
        )
        secretNamesByEnv;

      secretsMap = lib.mapAttrs
        (
          _: secretName: config.sops.secrets.${secretName}.path
        )
        availableSecretNamesByEnv;

      hasAnySecrets = secretsMap != { };

      fishSecretCommandCases = lib.concatStringsSep "\n" (
        lib.mapAttrsToList
          (name: path: ''
            case ${name}
              set path ${path}'')
          secretsMap
      );
    in
    {
      home.packages = with pkgs; [
        fish
        dash
        starship
        zoxide
        atuin
        navi
        tealdeer
      ];

      home.activation.generateFishSecrets = lib.mkIf hasAnySecrets (
        lib.hm.dag.entryAfter [ "writeBoundary" ] ''
          if [ -n "''${oldGenPath:-}" ] && [ "''${oldGenPath}" = "''${newGenPath:-}" ]; then
            echo "Home Manager generation unchanged, skipping Fish secrets generation"
          else
            $DRY_RUN_CMD mkdir -p ${config.home.homeDirectory}/.config/fish
            umask 077
            cat > ${config.home.homeDirectory}/.config/fish/private.fish << 'EOF'
          # Auto-generated by Home Manager. Secrets are only exported to the command
          # explicitly launched through this function.
          function with-secret
            if test (count $argv) -lt 2
              echo "Usage: with-secret ENV_VAR command [args...]" >&2
              return 2
            end

            set -l secret_name $argv[1]
            set -l path
            switch $secret_name
              ${fishSecretCommandCases}
              case '*'
                echo "Unknown secret: $secret_name" >&2
                return 2
            end

            if not test -r "$path"
              echo "Secret is unavailable: $secret_name" >&2
              return 1
            end

            set -lx $secret_name (string trim < "$path")
            command $argv[2..-1]
          end
          EOF

            $DRY_RUN_CMD ${lib.getExe' pkgs.coreutils "chmod"} 600 ${config.home.homeDirectory}/.config/fish/private.fish

            if [ "$(id -u)" = "0" ]; then
              $DRY_RUN_CMD ${lib.getExe' pkgs.coreutils "chown"} ${config.home.username}:${config.home.username} ${config.home.homeDirectory}/.config/fish/private.fish
            fi
          fi
        ''
      );
    };
}
