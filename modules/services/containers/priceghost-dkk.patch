diff -ruN a/backend/src/routes/products.ts b/backend/src/routes/products.ts
--- a/backend/src/routes/products.ts	1970-01-01 01:00:01.000000000 +0100
+++ b/backend/src/routes/products.ts	2026-05-10 22:48:38.737780217 +0200
@@ -24,7 +24,7 @@
 router.post('/', async (req: AuthRequest, res: Response) => {
   try {
     const userId = req.userId!;
-    const { url, refresh_interval, selectedPrice, selectedMethod } = req.body;
+    const { url, refresh_interval, selectedPrice, selectedMethod, selectedCurrency } = req.body;
 
     if (!url) {
       res.status(400).json({ error: 'URL is required' });
@@ -65,7 +65,7 @@
       await priceHistoryQueries.create(
         product.id,
         selectedPrice,
-        'USD', // TODO: Get currency from selection
+        selectedCurrency || 'DKK',
         null
       );
 
diff -ruN a/backend/src/services/scraper.ts b/backend/src/services/scraper.ts
--- a/backend/src/services/scraper.ts	1970-01-01 01:00:01.000000000 +0100
+++ b/backend/src/services/scraper.ts	2026-05-10 22:48:38.742908166 +0200
@@ -198,19 +198,19 @@
       if (priceAmount) {
         const price = parseFloat(priceAmount);
         if (!isNaN(price) && price > 0) {
-          let currency = 'USD';
+          let currency = 'DKK';
           const textSources = [text, $el.parent().text(), $el.closest('.price-box').text()];
           for (const source of textSources) {
             if (!source) continue;
-            const currencyCodeMatch = source.match(/\b(CHF|EUR|GBP|USD|CAD|AUD|JPY|INR)\b/i);
+            const currencyCodeMatch = source.match(/\b(CHF|EUR|GBP|USD|CAD|AUD|JPY|INR|DKK)\b/i);
             if (currencyCodeMatch) {
               currency = currencyCodeMatch[1].toUpperCase();
               break;
             }
             const symbolMatch = source.match(/([$€£¥₹])/);
             if (symbolMatch) {
-              const symbolMap: Record<string, string> = { '$': 'USD', '€': 'EUR', '£': 'GBP', '¥': 'JPY', '₹': 'INR' };
-              currency = symbolMap[symbolMatch[1]] || 'USD';
+              const symbolMap: Record<string, string> = { '$': 'USD', '€': 'EUR', '£': 'GBP', '¥': 'JPY', '₹': 'INR', 'kr': 'DKK', 'kr.': 'DKK' };
+              currency = symbolMap[symbolMatch[1].toLowerCase()] || symbolMap[symbolMatch[1]] || 'DKK';
               break;
             }
           }
@@ -1016,16 +1016,16 @@
             const priceValue = parseFloat(priceAmount);
             if (!isNaN(priceValue) && priceValue > 0) {
               // Detect currency from the page
-              let currency = 'USD';
+              let currency = 'DKK';
               const priceText = el.closest('.price-box').text() || el.parent().text() || '';
-              const currencyMatch = priceText.match(/\b(CHF|EUR|GBP|USD|CAD|AUD)\b/i);
+              const currencyMatch = priceText.match(/\b(CHF|EUR|GBP|USD|CAD|AUD|DKK)\b/i);
               if (currencyMatch) {
                 currency = currencyMatch[1].toUpperCase();
               } else {
                 // Check for currency symbols
                 const symbolMatch = priceText.match(/([$€£])/);
                 if (symbolMatch) {
-                  currency = symbolMatch[1] === '€' ? 'EUR' : symbolMatch[1] === '£' ? 'GBP' : 'USD';
+                  currency = symbolMatch[1] === '€' ? 'EUR' : symbolMatch[1] === '£' ? 'GBP' : 'DKK';
                 }
               }
               price = { price: priceValue, currency };
@@ -1886,7 +1886,7 @@
         const price = parseFloat(priceAmount);
         if (!isNaN(price) && price > 0) {
           // Try to detect currency from nearby elements, parent, or page
-          let currency = 'USD';
+          let currency = 'DKK';
 
           // Look for currency in the element's text, parent, and price-box container
           const textSources = [
@@ -1900,7 +1900,7 @@
           for (const source of textSources) {
             if (!source) continue;
             // Look for known currency codes first (more specific)
-            const currencyCodeMatch = source.match(/\b(CHF|EUR|GBP|USD|CAD|AUD|JPY|INR)\b/i);
+            const currencyCodeMatch = source.match(/\b(CHF|EUR|GBP|USD|CAD|AUD|JPY|INR|DKK)\b/i);
             if (currencyCodeMatch) {
               currency = currencyCodeMatch[1].toUpperCase();
               break;
@@ -1908,8 +1908,8 @@
             // Then try currency symbols
             const symbolMatch = source.match(/([$€£¥₹])/);
             if (symbolMatch) {
-              const symbolMap: Record<string, string> = { '$': 'USD', '€': 'EUR', '£': 'GBP', '¥': 'JPY', '₹': 'INR' };
-              currency = symbolMap[symbolMatch[1]] || 'USD';
+              const symbolMap: Record<string, string> = { '$': 'USD', '€': 'EUR', '£': 'GBP', '¥': 'JPY', '₹': 'INR', 'kr': 'DKK', 'kr.': 'DKK' };
+              currency = symbolMap[symbolMatch[1].toLowerCase()] || symbolMap[symbolMatch[1]] || 'DKK';
               break;
             }
           }
diff -ruN a/backend/src/utils/priceParser.ts b/backend/src/utils/priceParser.ts
--- a/backend/src/utils/priceParser.ts	1970-01-01 01:00:01.000000000 +0100
+++ b/backend/src/utils/priceParser.ts	2026-05-10 22:48:38.739895131 +0200
@@ -10,6 +10,9 @@
   '£': 'GBP',
   '¥': 'JPY',
   '₹': 'INR',
+  'kr': 'DKK',
+  'kr.': 'DKK',
+  'DKK': 'DKK',
   'Fr.': 'CHF',
   'CHF': 'CHF',
   'CAD': 'CAD',
@@ -22,11 +25,11 @@
 // Patterns to match prices in text
 const pricePatterns = [
   // $29.99 or $29,99 or $ 29.99
-  /(?<currency>[$€£¥₹])\s*(?<price>[\d,]+\.?\d*)/,
+  /(?<currency>[$€£¥₹]|kr\.?|DKK)\s*(?<price>[\d.,\s]+)/i,
   // CHF 29.99 or Fr. 29.99 (Swiss franc prefix)
   /(?<currency>CHF|Fr\.)\s*(?<price>[\d,]+\.?\d*)/i,
   // 29.99 USD or 29,99 EUR or 29.99 CHF
-  /(?<price>[\d,]+\.?\d*)\s*(?<currency>USD|EUR|GBP|CAD|AUD|JPY|INR|CHF)/i,
+  /(?<price>[\d.,\s]+)\s*(?<currency>USD|EUR|GBP|CAD|AUD|JPY|INR|CHF|DKK|kr\.?)/i,
   // Plain number with optional decimal (fallback)
   /(?<price>\d{1,3}(?:[,.\s]?\d{3})*(?:[.,]\d{2})?)/,
 ];
@@ -60,7 +63,7 @@
       if (priceStr) {
         const price = normalizePrice(priceStr);
         if (price !== null && price > 0) {
-          const currency = currencyMap[currencySymbol] || 'USD';
+          const currency = currencyMap[currencySymbol] || currencyMap[currencySymbol.toLowerCase()] || currencyMap[currencySymbol.toUpperCase()] || 'DKK';
           return { price, currency };
         }
       }
@@ -72,7 +75,7 @@
   if (numberMatch) {
     const price = normalizePrice(numberMatch[0]);
     if (price !== null && price > 0) {
-      return { price, currency: 'USD' };
+      return { price, currency: 'DKK' };
     }
   }
 
@@ -107,7 +110,7 @@
 
   // Match all price-like patterns in the HTML
   const allMatches = html.matchAll(
-    /(?:[$€£¥₹])\s*[\d,]+\.?\d*|(?:CHF|Fr\.)\s*[\d,]+\.?\d*|[\d,]+\.?\d*\s*(?:USD|EUR|GBP|CAD|AUD|CHF)/gi
+    /(?:[$€£¥₹]|kr\.?|DKK)\s*[\d.,\s]+|(?:CHF|Fr\.)\s*[\d,]+\.?\d*|[\d.,\s]+\s*(?:USD|EUR|GBP|CAD|AUD|CHF|DKK|kr\.?)/gi
   );
 
   for (const match of allMatches) {
diff -ruN a/frontend/src/api/client.ts b/frontend/src/api/client.ts
--- a/frontend/src/api/client.ts	1970-01-01 01:00:01.000000000 +0100
+++ b/frontend/src/api/client.ts	2026-05-10 22:48:38.751026924 +0200
@@ -120,12 +120,13 @@
 
   getById: (id: number) => api.get<ProductWithStats>(`/products/${id}`),
 
-  create: (url: string, refreshInterval?: number, selectedPrice?: number, selectedMethod?: string) =>
+  create: (url: string, refreshInterval?: number, selectedPrice?: number, selectedMethod?: string, selectedCurrency?: string) =>
     api.post<CreateProductResponse>('/products', {
       url,
       refresh_interval: refreshInterval,
       selectedPrice,
       selectedMethod,
+      selectedCurrency,
     }),
 
   update: (id: number, data: {
diff -ruN a/frontend/src/components/NotificationBell.tsx b/frontend/src/components/NotificationBell.tsx
--- a/frontend/src/components/NotificationBell.tsx	1970-01-01 01:00:01.000000000 +0100
+++ b/frontend/src/components/NotificationBell.tsx	2026-05-10 22:48:38.743704064 +0200
@@ -51,7 +51,7 @@
   if (price === null || price === undefined) return '';
   const numPrice = typeof price === 'string' ? parseFloat(price) : price;
   if (isNaN(numPrice)) return '';
-  const symbol = currency === 'EUR' ? '\u20AC' : currency === 'GBP' ? '\u00A3' : '$';
+  const symbol = currency === 'EUR' ? '\u20AC' : currency === 'GBP' ? '\u00A3' : currency === 'DKK' ? 'kr. ' : '$';
   return `${symbol}${numPrice.toFixed(2)}`;
 }
 
diff -ruN a/frontend/src/components/PriceChart.tsx b/frontend/src/components/PriceChart.tsx
--- a/frontend/src/components/PriceChart.tsx	1970-01-01 01:00:01.000000000 +0100
+++ b/frontend/src/components/PriceChart.tsx	2026-05-10 22:48:38.744212253 +0200
@@ -59,7 +59,7 @@
   };
 
   const currencySymbol =
-    currency === 'EUR' ? '€' : currency === 'GBP' ? '£' : currency === 'CHF' ? 'CHF ' : '$';
+    currency === 'EUR' ? '€' : currency === 'GBP' ? '£' : currency === 'CHF' ? 'CHF ' : currency === 'DKK' ? 'kr. ' : '$';
 
   const chartData = prices.map((p) => ({
     date: new Date(p.recorded_at).getTime(),
diff -ruN a/frontend/src/components/PriceSelectionModal.tsx b/frontend/src/components/PriceSelectionModal.tsx
--- a/frontend/src/components/PriceSelectionModal.tsx	1970-01-01 01:00:01.000000000 +0100
+++ b/frontend/src/components/PriceSelectionModal.tsx	2026-05-10 22:48:38.747794502 +0200
@@ -11,7 +11,7 @@
 interface PriceSelectionModalProps {
   isOpen: boolean;
   onClose: () => void;
-  onSelect: (price: number, method: string) => void;
+  onSelect: (price: number, method: string, currency: string) => void;
   productName: string | null;
   imageUrl: string | null;
   candidates: PriceCandidate[];
@@ -57,14 +57,14 @@
     const selected = candidates[selectedIndex];
     setIsSubmitting(true);
     try {
-      await onSelect(selected.price, selected.method);
+      await onSelect(selected.price, selected.method, selected.currency);
     } finally {
       setIsSubmitting(false);
     }
   };
 
   const formatPrice = (price: number, currency: string) => {
-    const symbol = currency === 'EUR' ? '€' : currency === 'GBP' ? '£' : currency === 'CHF' ? 'CHF ' : '$';
+    const symbol = currency === 'EUR' ? '€' : currency === 'GBP' ? '£' : currency === 'CHF' ? 'CHF ' : currency === 'DKK' ? 'kr. ' : '$';
     return `${symbol}${price.toFixed(2)}`;
   };
 
diff -ruN a/frontend/src/components/ProductCard.tsx b/frontend/src/components/ProductCard.tsx
--- a/frontend/src/components/ProductCard.tsx	1970-01-01 01:00:01.000000000 +0100
+++ b/frontend/src/components/ProductCard.tsx	2026-05-10 22:48:38.752184574 +0200
@@ -82,7 +82,7 @@
     const numPrice = typeof price === 'string' ? parseFloat(price) : price;
     if (isNaN(numPrice)) return 'N/A';
     const currencySymbol =
-      currency === 'EUR' ? '€' : currency === 'GBP' ? '£' : currency === 'CHF' ? 'CHF ' : '$';
+      currency === 'EUR' ? '€' : currency === 'GBP' ? '£' : currency === 'CHF' ? 'CHF ' : currency === 'DKK' ? 'kr. ' : '$';
     return `${currencySymbol}${numPrice.toFixed(2)}`;
   };
 
@@ -498,7 +498,7 @@
                   <path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9" />
                   <path d="M13.73 21a2 2 0 0 1-3.46 0" />
                 </svg>
-                ${Number(product.price_drop_threshold).toFixed(2)} drop
+                {formatPrice(product.price_drop_threshold, product.currency)} drop
               </span>
             )}
             {product.target_price && (
@@ -508,7 +508,7 @@
                   <circle cx="12" cy="12" r="6" />
                   <circle cx="12" cy="12" r="2" />
                 </svg>
-                Target: ${Number(product.target_price).toFixed(2)}
+                Target: {formatPrice(product.target_price, product.currency)}
               </span>
             )}
             {product.notify_back_in_stock && (
diff -ruN a/frontend/src/pages/Dashboard.tsx b/frontend/src/pages/Dashboard.tsx
--- a/frontend/src/pages/Dashboard.tsx	1970-01-01 01:00:01.000000000 +0100
+++ b/frontend/src/pages/Dashboard.tsx	2026-05-10 22:48:38.749908098 +0200
@@ -83,14 +83,15 @@
     setProducts((prev) => [response.data as Product, ...prev]);
   };
 
-  const handlePriceSelected = async (selectedPrice: number, selectedMethod: string) => {
+  const handlePriceSelected = async (selectedPrice: number, selectedMethod: string, selectedCurrency: string) => {
     if (!priceReviewData) return;
 
     const response = await productsApi.create(
       priceReviewData.url,
       pendingRefreshInterval,
       selectedPrice,
-      selectedMethod
+      selectedMethod,
+      selectedCurrency
     );
 
     // When selecting a price, the API should always return a Product
diff -ruN a/frontend/src/pages/NotificationHistory.tsx b/frontend/src/pages/NotificationHistory.tsx
--- a/frontend/src/pages/NotificationHistory.tsx	1970-01-01 01:00:01.000000000 +0100
+++ b/frontend/src/pages/NotificationHistory.tsx	2026-05-10 22:48:38.745791687 +0200
@@ -44,7 +44,7 @@
   if (price === null || price === undefined) return '-';
   const numPrice = typeof price === 'string' ? parseFloat(price) : price;
   if (isNaN(numPrice)) return '-';
-  const symbol = currency === 'EUR' ? '\u20AC' : currency === 'GBP' ? '\u00A3' : currency === 'CHF' ? 'CHF ' : '$';
+  const symbol = currency === 'EUR' ? '\u20AC' : currency === 'GBP' ? '\u00A3' : currency === 'CHF' ? 'CHF ' : currency === 'DKK' ? 'kr. ' : '$';
   return `${symbol}${numPrice.toFixed(2)}`;
 }
 
diff -ruN a/frontend/src/pages/ProductDetail.tsx b/frontend/src/pages/ProductDetail.tsx
--- a/frontend/src/pages/ProductDetail.tsx	1970-01-01 01:00:01.000000000 +0100
+++ b/frontend/src/pages/ProductDetail.tsx	2026-05-10 22:48:38.746756625 +0200
@@ -167,7 +167,7 @@
     const numPrice = typeof price === 'string' ? parseFloat(price) : price;
     if (isNaN(numPrice)) return 'N/A';
     const currencySymbol =
-      currency === 'EUR' ? '€' : currency === 'GBP' ? '£' : currency === 'CHF' ? 'CHF ' : '$';
+      currency === 'EUR' ? '€' : currency === 'GBP' ? '£' : currency === 'CHF' ? 'CHF ' : currency === 'DKK' ? 'kr. ' : '$';
     return `${currencySymbol}${numPrice.toFixed(2)}`;
   };
 
