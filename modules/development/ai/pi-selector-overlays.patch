--- a/docs/settings.md
+++ b/docs/settings.md
@@ -65,6 +65,8 @@
 | `editorPaddingX` | number | `0` | Horizontal padding for input editor (0-3) |
 | `outputPad` | number | `1` | Horizontal padding for user messages, assistant messages, and thinking (0 or 1) |
 | `autocompleteMaxVisible` | number | `5` | Max visible items in autocomplete dropdown (3-20) |
+| `selectorOverlayWidth` | number | `72` | Default selector modal width in terminal columns (minimum 40). Clamped to the terminal width |
+| `selectorOverlayWidths` | object | `{}` | Per-selector width overrides for `settings`, `thinking`, `trust`, `model`, `scoped-models`, `fork`, `tree`, `session`, and `auth` |
 | `showHardwareCursor` | boolean | `false` | Show the terminal cursor while TUI positions it for IME support |
 | `tuiMode` | string | `"regular"` | Interactive TUI mode: `"regular"` or experimental `"fullscreen"`. Changes from `/settings` apply immediately; `--tui-mode` overrides this setting at startup |
 | `fullscreenExitOutput` | string | `"transcript"` | Fullscreen exit output: `"transcript"` prints the final transcript and resume hint, while `"resume-hint"` restores the previous screen and prints only the resume hint. Has no effect in regular TUI mode |
--- a/dist/core/settings-manager.d.ts
+++ b/dist/core/settings-manager.d.ts
@@ -23,6 +23,9 @@
 }
 export type TuiMode = RendererTuiMode;
 export type FullscreenExitOutput = "transcript" | "resume-hint";
+export type SelectorOverlayName = "settings" | "thinking" | "trust" | "model" | "scoped-models" | "fork" | "tree" | "session" | "auth";
+export declare const DEFAULT_SELECTOR_OVERLAY_WIDTH = 72;
+export declare const MIN_SELECTOR_OVERLAY_WIDTH = 40;
 export interface TerminalSettings {
     showImages?: boolean;
     imageWidthCells?: number;
@@ -107,6 +110,8 @@
     editorPaddingX?: number;
     outputPad?: 0 | 1;
     autocompleteMaxVisible?: number;
+    selectorOverlayWidth?: number;
+    selectorOverlayWidths?: Partial<Record<SelectorOverlayName, number>>;
     showHardwareCursor?: boolean;
     markdown?: MarkdownSettings;
     warnings?: WarningSettings;
@@ -322,6 +327,8 @@
     setOutputPad(padding: 0 | 1): void;
     getAutocompleteMaxVisible(): number;
     setAutocompleteMaxVisible(maxVisible: number): void;
+    getSelectorOverlayWidth(selector?: SelectorOverlayName): number;
+    setSelectorOverlayWidth(width: number): void;
     getCodeBlockIndent(): string;
     getMermaidRenderingMode(): MermaidRenderingMode;
     setMermaidRenderingMode(mode: MermaidRenderingMode): void;
--- a/dist/core/settings-manager.js
+++ b/dist/core/settings-manager.js
@@ -6,6 +6,8 @@
 import { normalizePath, resolvePath } from "../utils/paths.js";
 import { stripBom } from "../utils/text.js";
 import { DEFAULT_HTTP_IDLE_TIMEOUT_MS, parseHttpIdleTimeoutMs } from "./http-dispatcher.js";
+export const DEFAULT_SELECTOR_OVERLAY_WIDTH = 72;
+export const MIN_SELECTOR_OVERLAY_WIDTH = 40;
 function isMergeableObject(value) {
     return typeof value === "object" && value !== null && !Array.isArray(value);
 }
@@ -38,6 +40,11 @@
     }
     return undefined;
 }
+function normalizeSelectorOverlayWidth(width) {
+    if (typeof width !== "number" || Number.isFinite(width) === false)
+        return DEFAULT_SELECTOR_OVERLAY_WIDTH;
+    return Math.max(MIN_SELECTOR_OVERLAY_WIDTH, Math.floor(width));
+}
 function toSettingsError(scope, error, path) {
     return {
         scope,
@@ -972,6 +979,18 @@
         this.markModified("autocompleteMaxVisible");
         this.save();
     }
+    getSelectorOverlayWidth(selector) {
+        const selectorWidth = selector ? this.settings.selectorOverlayWidths?.[selector] : undefined;
+        if (typeof selectorWidth === "number" && Number.isFinite(selectorWidth)) {
+            return normalizeSelectorOverlayWidth(selectorWidth);
+        }
+        return normalizeSelectorOverlayWidth(this.settings.selectorOverlayWidth);
+    }
+    setSelectorOverlayWidth(width) {
+        this.globalSettings.selectorOverlayWidth = normalizeSelectorOverlayWidth(width);
+        this.markModified("selectorOverlayWidth");
+        this.save();
+    }
     getCodeBlockIndent() {
         return this.settings.markdown?.codeBlockIndent ?? "  ";
     }
--- a/dist/modes/interactive/components/settings-selector.d.ts
+++ b/dist/modes/interactive/components/settings-selector.d.ts
@@ -1,7 +1,7 @@
 import type { ThinkingLevel } from "@earendil-works/pi-agent-core";
 import { type Model, type Transport } from "@earendil-works/pi-ai";
 import { Container, type ScrollViewScrollbar, SettingsList } from "@earendil-works/pi-tui";
-import type { DefaultProjectTrust, FullscreenExitOutput, MermaidRenderingMode, TuiMode, WarningSettings } from "../../../core/settings-manager.ts";
+import { type DefaultProjectTrust, type FullscreenExitOutput, type MermaidRenderingMode, type TuiMode, type WarningSettings } from "../../../core/settings-manager.ts";
 import { type TerminalTheme } from "../theme/theme.ts";
 export interface SettingsConfig {
     autoCompact: boolean;
@@ -34,6 +34,7 @@
     editorPaddingX: number;
     outputPad: 0 | 1;
     autocompleteMaxVisible: number;
+    selectorOverlayWidth?: number;
     quietStartup: boolean;
     defaultProjectTrust: DefaultProjectTrust;
     clearOnShrink: boolean;
@@ -70,6 +71,7 @@
     onEditorPaddingXChange: (padding: number) => void;
     onOutputPadChange: (padding: 0 | 1) => void;
     onAutocompleteMaxVisibleChange: (maxVisible: number) => void;
+    onSelectorOverlayWidthChange?: (width: number) => void;
     onQuietStartupChange: (enabled: boolean) => void;
     onDefaultProjectTrustChange: (defaultProjectTrust: DefaultProjectTrust) => void;
     onClearOnShrinkChange: (enabled: boolean) => void;
--- a/dist/modes/interactive/components/settings-selector.js
+++ b/dist/modes/interactive/components/settings-selector.js
@@ -1,6 +1,7 @@
 import { getSupportedThinkingLevels } from "@earendil-works/pi-ai";
 import { Container, getCapabilities, SettingsList, Spacer, Text, } from "@earendil-works/pi-tui";
 import { formatHttpIdleTimeoutMs, HTTP_IDLE_TIMEOUT_CHOICES } from "../../../core/http-dispatcher.js";
+import { DEFAULT_SELECTOR_OVERLAY_WIDTH, } from "../../../core/settings-manager.js";
 import { getSettingsListTheme, parseAutoThemeSetting, theme } from "../theme/theme.js";
 import { DynamicBorder } from "./dynamic-border.js";
 import { keyDisplayText } from "./keybinding-hints.js";
@@ -573,9 +574,17 @@
             currentValue: String(config.autocompleteMaxVisible),
             values: ["3", "5", "7", "10", "15", "20"],
         });
-        // Clear on shrink toggle (insert after autocomplete-max-visible)
+        // Default selector modal width (insert after autocomplete-max-visible)
         const autocompleteIndex = items.findIndex((item) => item.id === "autocomplete-max-visible");
         items.splice(autocompleteIndex + 1, 0, {
+            id: "selector-overlay-width",
+            label: "Default selector modal width",
+            description: "Fallback width in terminal columns; per-selector JSON overrides take precedence",
+            currentValue: String(config.selectorOverlayWidth ?? DEFAULT_SELECTOR_OVERLAY_WIDTH),
+            values: ["72", "80", "100", "120"],
+        });
+        // Clear on shrink toggle (insert after selector-overlay-width)
+        items.splice(autocompleteIndex + 2, 0, {
             id: "clear-on-shrink",
             label: "Clear on shrink",
             description: "Clear empty rows when content shrinks (may cause flicker)",
@@ -672,6 +681,9 @@
                 case "autocomplete-max-visible":
                     callbacks.onAutocompleteMaxVisibleChange(parseInt(newValue, 10));
                     break;
+                case "selector-overlay-width":
+                    callbacks.onSelectorOverlayWidthChange?.(parseInt(newValue, 10));
+                    break;
                 case "clear-on-shrink":
                     callbacks.onClearOnShrinkChange(newValue === "true");
                     break;
--- a/dist/modes/interactive/components/tree-selector.d.ts
+++ b/dist/modes/interactive/components/tree-selector.d.ts
@@ -23,6 +23,7 @@
     onCopy?: (text: string | undefined) => void;
     onLabelEdit?: (entryId: string, currentLabel: string | undefined) => void;
     constructor(tree: SessionTreeNode[], currentLeafId: string | null, maxVisibleLines: number, initialSelectedId?: string, initialFilterMode?: FilterMode);
+    setMaxVisibleLines(maxVisibleLines: number): void;
     /**
      * Find the index of the nearest visible entry, walking up the parent chain if needed.
      * Returns the index in filteredNodes, or the last index as fallback.
@@ -79,12 +80,15 @@
     private labelInput;
     private labelInputContainer;
     private treeContainer;
+    private treeHelp;
     private onLabelChangeCallback?;
+    private readonly getViewportHeight?;
     onCopy?: (text: string | undefined) => void;
     private _focused;
     get focused(): boolean;
     set focused(value: boolean);
-    constructor(tree: SessionTreeNode[], currentLeafId: string | null, terminalHeight: number, onSelect: (entryId: string) => void, onCancel: () => void, onLabelChange?: (entryId: string, label: string | undefined) => void, initialSelectedId?: string, initialFilterMode?: FilterMode);
+    constructor(tree: SessionTreeNode[], currentLeafId: string | null, terminalHeight: number, onSelect: (entryId: string) => void, onCancel: () => void, onLabelChange?: (entryId: string, label: string | undefined) => void, initialSelectedId?: string, initialFilterMode?: FilterMode, getViewportHeight?: () => number);
+    render(width: number): string[];
     private showLabelInput;
     private hideLabelInput;
     handleInput(keyData: string): void;
--- a/dist/modes/interactive/components/tree-selector.js
+++ b/dist/modes/interactive/components/tree-selector.js
@@ -69,6 +69,9 @@
         this.selectedIndex = this.findNearestVisibleIndex(targetId);
         this.lastSelectedId = this.filteredNodes[this.selectedIndex]?.node.entry.id ?? null;
     }
+    setMaxVisibleLines(maxVisibleLines) {
+        this.maxVisibleLines = Math.max(1, maxVisibleLines);
+    }
     /**
      * Find the index of the nearest visible entry, walking up the parent chain if needed.
      * Returns the index in filteredNodes, or the last index as fallback.
@@ -1032,8 +1035,14 @@
 }
 /** Component that renders tree help as semantic rows with chunk-aware wrapping */
 class TreeHelp {
+    visible = true;
+    setVisible(visible) {
+        this.visible = visible;
+    }
     invalidate() { }
     render(width) {
+        if (!this.visible)
+            return [];
         const items = TREE_HELP_ITEMS.map(({ keys, label, labelFirst }) => {
             const text = formatHelpKeys(keys);
             if (!text)
@@ -1169,7 +1178,9 @@
     labelInput = null;
     labelInputContainer;
     treeContainer;
+    treeHelp;
     onLabelChangeCallback;
+    getViewportHeight;
     onCopy;
     // Focusable implementation - propagate to labelInput when active for IME cursor positioning
     _focused = false;
@@ -1183,9 +1194,10 @@
             this.labelInput.focused = value;
         }
     }
-    constructor(tree, currentLeafId, terminalHeight, onSelect, onCancel, onLabelChange, initialSelectedId, initialFilterMode) {
+    constructor(tree, currentLeafId, terminalHeight, onSelect, onCancel, onLabelChange, initialSelectedId, initialFilterMode, getViewportHeight) {
         super();
         this.onLabelChangeCallback = onLabelChange;
+        this.getViewportHeight = getViewportHeight;
         const maxVisibleLines = Math.max(5, Math.floor(terminalHeight / 2));
         this.treeList = new TreeList(tree, currentLeafId, maxVisibleLines, initialSelectedId, initialFilterMode);
         this.treeList.onSelect = onSelect;
@@ -1198,7 +1210,8 @@
         this.addChild(new Spacer(1));
         this.addChild(new DynamicBorder());
         this.addChild(new Text(theme.bold("  Session Tree"), 1, 0));
-        this.addChild(new TreeHelp());
+        this.treeHelp = new TreeHelp();
+        this.addChild(this.treeHelp);
         this.addChild(new SearchLine(this.treeList));
         this.addChild(new DynamicBorder());
         this.addChild(new Spacer(1));
@@ -1210,6 +1223,44 @@
             setTimeout(() => onCancel(), 100);
         }
     }
+    render(width) {
+        if (!this.getViewportHeight)
+            return super.render(width);
+        const viewportHeight = this.getViewportHeight();
+        this.treeHelp.setVisible(true);
+        let initialLines = super.render(width);
+        if (this.labelInput) {
+            if (initialLines.length > viewportHeight) {
+                this.treeHelp.setVisible(false);
+                initialLines = super.render(width);
+            }
+            if (initialLines.length <= viewportHeight)
+                return initialLines;
+            return this.labelInput.render(width).slice(0, viewportHeight);
+        }
+        let chromeRows = initialLines.length - this.treeList.render(width).length;
+        if (chromeRows >= viewportHeight) {
+            this.treeHelp.setVisible(false);
+            initialLines = super.render(width);
+            chromeRows = initialLines.length - this.treeList.render(width).length;
+        }
+        if (chromeRows >= viewportHeight) {
+            // On extremely short terminals, prioritize the focused list over optional modal chrome.
+            this.treeList.setMaxVisibleLines(1);
+            return this.treeList.render(width).slice(0, viewportHeight);
+        }
+        let maxVisibleLines = viewportHeight - chromeRows;
+        let rendered = initialLines;
+        for (let attempt = 0; attempt < 2; attempt++) {
+            this.treeList.setMaxVisibleLines(maxVisibleLines);
+            rendered = super.render(width);
+            const overflow = rendered.length - viewportHeight;
+            if (overflow <= 0)
+                return rendered;
+            maxVisibleLines -= overflow;
+        }
+        return rendered;
+    }
     showLabelInput(entryId, currentLabel) {
         this.labelInput = new LabelInput(entryId, currentLabel);
         this.labelInput.onSubmit = (id, label) => {
--- a/dist/modes/interactive/components/user-message-selector.d.ts
+++ b/dist/modes/interactive/components/user-message-selector.d.ts
@@ -16,6 +16,7 @@
     constructor(messages: UserMessageItem[], initialSelectedId?: string);
     invalidate(): void;
     render(width: number): string[];
+    setMaxVisible(maxVisible: number): void;
     handleInput(keyData: string): void;
 }
 /**
@@ -23,7 +24,9 @@
  */
 export declare class UserMessageSelectorComponent extends Container {
     private messageList;
-    constructor(messages: UserMessageItem[], onSelect: (entryId: string) => void, onCancel: () => void, initialSelectedId?: string);
+    private readonly getViewportHeight?;
+    constructor(messages: UserMessageItem[], onSelect: (entryId: string) => void, onCancel: () => void, initialSelectedId?: string, getViewportHeight?: () => number);
+    render(width: number): string[];
     getMessageList(): UserMessageList;
 }
 export {};
--- a/dist/modes/interactive/components/user-message-selector.js
+++ b/dist/modes/interactive/components/user-message-selector.js
@@ -1,6 +1,8 @@
 import { Container, getKeybindings, Spacer, Text, truncateToWidth } from "@earendil-works/pi-tui";
 import { theme } from "../theme/theme.js";
 import { DynamicBorder } from "./dynamic-border.js";
+const DEFAULT_MAX_VISIBLE_MESSAGES = 10;
+const MESSAGE_ROWS = 3;
 /**
  * Custom user message list component with selection
  */
@@ -9,7 +11,7 @@
     selectedIndex = 0;
     onSelect;
     onCancel;
-    maxVisible = 10; // Max messages visible
+    maxVisible = DEFAULT_MAX_VISIBLE_MESSAGES;
     constructor(messages, initialSelectedId) {
         // Store messages in chronological order (oldest to newest)
         this.messages = messages;
@@ -22,13 +24,14 @@
     }
     render(width) {
         const lines = [];
+        const maxVisible = this.maxVisible;
         if (this.messages.length === 0) {
             lines.push(theme.fg("muted", "  No user messages found"));
             return lines;
         }
         // Calculate visible range with scrolling
-        const startIndex = Math.max(0, Math.min(this.selectedIndex - Math.floor(this.maxVisible / 2), this.messages.length - this.maxVisible));
-        const endIndex = Math.min(startIndex + this.maxVisible, this.messages.length);
+        const startIndex = Math.max(0, Math.min(this.selectedIndex - Math.floor(maxVisible / 2), this.messages.length - maxVisible));
+        const endIndex = Math.min(startIndex + maxVisible, this.messages.length);
         // Render visible messages (2 lines per message + blank line)
         for (let i = startIndex; i < endIndex; i++) {
             const message = this.messages[i];
@@ -55,6 +58,9 @@
         }
         return lines;
     }
+    setMaxVisible(maxVisible) {
+        this.maxVisible = Math.max(1, Math.min(DEFAULT_MAX_VISIBLE_MESSAGES, maxVisible));
+    }
     handleInput(keyData) {
         const kb = getKeybindings();
         // Up arrow - go to previous (older) message, wrap to bottom when at top
@@ -85,8 +91,10 @@
  */
 export class UserMessageSelectorComponent extends Container {
     messageList;
-    constructor(messages, onSelect, onCancel, initialSelectedId) {
+    getViewportHeight;
+    constructor(messages, onSelect, onCancel, initialSelectedId, getViewportHeight) {
         super();
+        this.getViewportHeight = getViewportHeight;
         // Add header
         this.addChild(new Spacer(1));
         this.addChild(new Text(theme.bold("Fork from Message"), 1, 0));
@@ -107,6 +115,29 @@
             setTimeout(() => onCancel(), 100);
         }
     }
+    render(width) {
+        if (!this.getViewportHeight)
+            return super.render(width);
+        const viewportHeight = this.getViewportHeight();
+        this.messageList.setMaxVisible(DEFAULT_MAX_VISIBLE_MESSAGES);
+        let rendered = super.render(width);
+        if (rendered.length <= viewportHeight)
+            return rendered;
+        const listRows = this.messageList.render(width).length;
+        const chromeRows = rendered.length - listRows;
+        const availableListRows = viewportHeight - chromeRows;
+        let maxVisible = Math.floor(availableListRows / MESSAGE_ROWS);
+        for (let attempt = 0; attempt < 2 && maxVisible > 0; attempt++) {
+            this.messageList.setMaxVisible(maxVisible);
+            rendered = super.render(width);
+            if (rendered.length <= viewportHeight)
+                return rendered;
+            maxVisible--;
+        }
+        // On extremely short terminals, prioritize the focused list over optional modal chrome.
+        this.messageList.setMaxVisible(1);
+        return this.messageList.render(width).slice(0, viewportHeight);
+    }
     getMessageList() {
         return this.messageList;
     }
--- a/dist/modes/interactive/interactive-mode.js
+++ b/dist/modes/interactive/interactive-mode.js
@@ -7,7 +7,7 @@
 import * as os from "node:os";
 import * as path from "node:path";
 import * as TuiLayouts from "@earendil-works/pi-tui";
-import { CombinedAutocompleteProvider, Container, fuzzyFilter, getCapabilities, hyperlink, Markdown, matchesKey, ProcessTerminal, Spacer, setCapabilityOverrides, setKeybindings, Text, TruncatedText, TuiAltScreen, TuiMainScreen, visibleWidth, } from "@earendil-works/pi-tui";
+import { CombinedAutocompleteProvider, Container, fuzzyFilter, getCapabilities, hyperlink, isFocusable, Markdown, matchesKey, ProcessTerminal, Spacer, setCapabilityOverrides, setKeybindings, Text, TruncatedText, TuiAltScreen, TuiMainScreen, visibleWidth, } from "@earendil-works/pi-tui";
 import chalk from "chalk";
 import { spawn } from "child_process";
 import { APP_NAME, APP_TITLE, CONFIG_DIR_NAME, getAgentDir, getAuthPath, getDebugLogPath, getDocsPath, VERSION, } from "../../config.js";
@@ -79,6 +79,66 @@
 function isExpandable(obj) {
     return typeof obj === "object" && obj !== null && "setExpanded" in obj && typeof obj.setExpanded === "function";
 }
+const SELECTOR_OVERLAY_OPTIONS = { anchor: "center", maxHeight: "100%", margin: 1 };
+const LOGIN_PROMPT_CANCEL = Symbol("loginPromptCancel");
+class SelectorOverlayComponent {
+    wantsKeyRelease;
+    component;
+    focus;
+    _focused = false;
+    constructor(component, focus) {
+        this.component = component;
+        this.focus = focus;
+        this.wantsKeyRelease = focus.wantsKeyRelease ?? false;
+    }
+    get focused() {
+        return this._focused;
+    }
+    set focused(value) {
+        this._focused = value;
+        if (isFocusable(this.focus))
+            this.focus.focused = value;
+    }
+    render(width) {
+        const paddingX = width > 2 ? 1 : 0;
+        const contentWidth = Math.max(1, width - paddingX * 2);
+        const background = theme.getBgAnsi("customMessageBg");
+        return this.component.render(contentWidth).map((line) => {
+            const panelLine = line
+                .replaceAll("\x1b[0m", `\x1b[0m${background}`)
+                .replaceAll("\x1b[49m", `\x1b[49m${background}`);
+            const fillWidth = Math.max(0, contentWidth - visibleWidth(line));
+            return `${background}${" ".repeat(paddingX)}${panelLine}${" ".repeat(fillWidth + paddingX)}\x1b[49m`;
+        });
+    }
+    handleInput(data) {
+        this.focus.handleInput?.(data);
+    }
+    invalidate() {
+        this.component.invalidate();
+    }
+}
+const SELECTOR_OVERLAY_NAMES = new Map([
+    [SettingsSelectorComponent, "settings"],
+    [ThinkingSelectorComponent, "thinking"],
+    [TrustSelectorComponent, "trust"],
+    [ModelSelectorComponent, "model"],
+    [ScopedModelsSelectorComponent, "scoped-models"],
+    [UserMessageSelectorComponent, "fork"],
+    [TreeSelectorComponent, "tree"],
+    [SessionSelectorComponent, "session"],
+    // Built-in showSelector uses this generic component for auth method selection.
+    [ExtensionSelectorComponent, "auth"],
+    [LoginDialogComponent, "auth"],
+    [OAuthSelectorComponent, "auth"],
+]);
+function getSelectorOverlayName(component) {
+    for (const [ComponentType, name] of SELECTOR_OVERLAY_NAMES) {
+        if (component instanceof ComponentType)
+            return name;
+    }
+    return undefined;
+}
 class ExpandableText extends Text {
     getCollapsedText;
     getExpandedText;
@@ -3721,39 +3781,79 @@
         dispose?.();
     }
     /**
-     * Shows a selector component in place of the editor.
+     * Shows a selector component as a modal overlay.
      * @param create Factory that receives a `done` callback and returns the component and focus target
      */
     showSelector(create) {
         const token = {};
         let dispose;
-        const done = () => {
+        let closeOverlay = () => { };
+        let disposed = false;
+        const teardown = () => {
+            if (disposed)
+                return;
+            disposed = true;
             dispose?.();
+            closeOverlay();
+        };
+        const done = () => {
+            teardown();
             if (this.activeSelectorToken !== token)
                 return;
             this.activeSelectorToken = undefined;
             this.activeSelectorDispose = undefined;
-            this.editorContainer.clear();
-            this.editorContainer.addChild(this.editor);
-            this.ui.setFocus(this.editor);
         };
         const created = create(done);
         dispose = created.dispose;
+        if (disposed) {
+            dispose?.();
+            return;
+        }
         this.disposeActiveSelector();
         this.activeSelectorToken = token;
-        this.activeSelectorDispose = dispose;
-        this.editorContainer.clear();
-        this.editorContainer.addChild(created.component);
-        this.ui.setFocus(created.focus);
-        this.ui.requestRender();
+        this.activeSelectorDispose = teardown;
+        const handle = this.showComponentOverlay(created.component, created.focus);
+        closeOverlay = () => handle.hide();
     }
+    showComponentOverlay(component, focus, selectorName = getSelectorOverlayName(component)) {
+        const overlay = new SelectorOverlayComponent(component, focus);
+        return this.ui.showOverlay(overlay, {
+            ...SELECTOR_OVERLAY_OPTIONS,
+            width: this.settingsManager.getSelectorOverlayWidth(selectorName),
+        });
+    }
+    showModalComponent(component, focus = component, onDismiss) {
+        let close = () => { };
+        let closing = false;
+        this.showSelector((done) => {
+            close = () => {
+                closing = true;
+                done();
+            };
+            return {
+                component,
+                focus,
+                dispose: () => {
+                    component.dispose?.();
+                    if (!closing)
+                        onDismiss?.();
+                },
+            };
+        });
+        return () => close();
+    }
+    showLoginDialogModal(dialog) {
+        return this.showModalComponent(dialog, dialog, () => {
+            dialog[LOGIN_PROMPT_CANCEL]?.();
+            dialog.cancel();
+        });
+    }
     showSettingsSelector() {
         this.showSelector((done) => {
-            let selector;
             const defaultProvider = this.settingsManager.getDefaultProvider();
             const defaultModelId = this.settingsManager.getDefaultModel();
             const defaultModel = defaultProvider && defaultModelId ? `${defaultProvider}/${defaultModelId}` : "not set";
-            selector = new SettingsSelectorComponent({
+            const selector = new SettingsSelectorComponent({
                 autoCompact: this.session.autoCompactionEnabled,
                 defaultModel,
                 currentModel: this.session.model,
@@ -3785,6 +3885,7 @@
                 editorPaddingX: this.settingsManager.getEditorPaddingX(),
                 outputPad: this.settingsManager.getOutputPad(),
                 autocompleteMaxVisible: this.settingsManager.getAutocompleteMaxVisible(),
+                selectorOverlayWidth: this.settingsManager.getSelectorOverlayWidth(),
                 quietStartup: this.settingsManager.getQuietStartup(),
                 clearOnShrink: this.settingsManager.getClearOnShrink(),
                 showTerminalProgress: this.settingsManager.getShowTerminalProgress(),
@@ -3934,6 +4035,9 @@
                         this.editor.setAutocompleteMaxVisible(maxVisible);
                     }
                 },
+                onSelectorOverlayWidthChange: (width) => {
+                    this.settingsManager.setSelectorOverlayWidth(width);
+                },
                 onClearOnShrinkChange: (enabled) => {
                     this.settingsManager.setClearOnShrink(enabled);
                     this.ui.setClearOnShrink(enabled);
@@ -3945,8 +4049,8 @@
                     this.settingsManager.setShowTerminalProgress(enabled);
                 },
                 onTuiModeChange: (mode) => {
+                    done();
                     if (!this.switchTuiMode(mode)) {
-                        selector?.getSettingsList().updateValue("tui-mode", this.ui.mode);
                         this.showStatus("Close active overlays before changing TUI mode");
                         return;
                     }
@@ -4316,7 +4420,7 @@
             }, () => {
                 done();
                 this.ui.requestRender();
-            }, initialSelectedId);
+            }, initialSelectedId, () => Math.max(1, this.ui.terminal.rows - 2));
             return { component: selector, focus: selector.getMessageList() };
         });
     }
@@ -4441,7 +4545,7 @@
             }, (entryId, label) => {
                 this.sessionManager.appendLabelChange(entryId, label);
                 this.ui.requestRender();
-            }, initialSelectedId, initialFilterMode);
+            }, initialSelectedId, initialFilterMode, () => Math.max(1, this.ui.terminal.rows - 2));
             selector.onCopy = async (text) => {
                 if (!text) {
                     this.showError("Selected entry has no text to copy");
@@ -4796,48 +4900,31 @@
             .finally(() => clearTimeout(timeout));
     }
     showAmbientAuthDialog(providerOption) {
-        const restoreEditor = () => {
-            this.editorContainer.clear();
-            this.editorContainer.addChild(this.editor);
-            this.ui.setFocus(this.editor);
-            this.ui.requestRender();
-        };
-        const dialog = new LoginDialogComponent(this.ui, providerOption.id, () => restoreEditor(), providerOption.name, `${providerOption.name} setup`);
+        let closeDialog = () => { };
+        const dialog = new LoginDialogComponent(this.ui, providerOption.id, () => closeDialog(), providerOption.name, `${providerOption.name} setup`);
         dialog.showInfo(`${providerOption.method?.name ?? "Authentication"} is configured outside ${APP_NAME}.`, [], true);
-        this.editorContainer.clear();
-        this.editorContainer.addChild(dialog);
-        this.ui.setFocus(dialog);
-        this.ui.requestRender();
+        closeDialog = this.showLoginDialogModal(dialog);
     }
     async showApiKeyLoginDialog(providerId, providerName) {
         const previousModel = this.session.model;
         const dialog = new LoginDialogComponent(this.ui, providerId, (_success, _message) => {
             // Completion handled below
         }, providerName);
         if (providerId === "amazon-bedrock") {
             dialog.showDetails([
                 theme.fg("text", "You can also use an AWS profile, IAM keys, or role-based credentials."),
                 theme.fg("muted", "See:"),
                 theme.fg("accent", `  ${path.join(getDocsPath(), "providers.md")}`),
             ]);
         }
-        this.editorContainer.clear();
-        this.editorContainer.addChild(dialog);
-        this.ui.setFocus(dialog);
-        this.ui.requestRender();
-        const restoreEditor = () => {
-            this.editorContainer.clear();
-            this.editorContainer.addChild(this.editor);
-            this.ui.setFocus(this.editor);
-            this.ui.requestRender();
-        };
+        const restoreEditor = this.showLoginDialogModal(dialog);
         try {
             await this.loginProvider(dialog, providerId, "api_key");
             restoreEditor();
             await this.completeProviderAuthentication(providerId, providerName, "api_key", previousModel);
         }
         catch (error) {
             restoreEditor();
             const errorMsg = error instanceof Error ? error.message : String(error);
             if (error instanceof CredentialSynchronizationError) {
                 this.showError(`Saved API key for ${providerName}, but local model state could not be synchronized: ${errorMsg}`);
@@ -4847,103 +4934,99 @@
             }
         }
     }
     showAuthSelect(dialog, prompt) {
         return new Promise((resolve, reject) => {
-            const restoreDialog = () => {
-                this.editorContainer.clear();
-                this.editorContainer.addChild(dialog);
-                this.ui.setFocus(dialog);
-                this.ui.requestRender();
-            };
             const labels = prompt.options.map((option) => option.label);
-            const selector = new ExtensionSelectorComponent(prompt.message, labels, (optionLabel) => {
-                restoreDialog();
-                const id = prompt.options.find((option) => option.label === optionLabel)?.id;
-                if (id)
-                    resolve(id);
-                else
-                    reject(new Error("Login cancelled"));
-            }, () => {
-                restoreDialog();
-                reject(new Error("Login cancelled"));
-            });
-            this.editorContainer.clear();
-            this.editorContainer.addChild(selector);
-            this.ui.setFocus(selector);
-            this.ui.requestRender();
+            let handle;
+            const closeSelector = () => {
+                prompt.signal?.removeEventListener("abort", onAbort);
+                handle?.hide();
+                dialog[LOGIN_PROMPT_CANCEL] = undefined;
+            };
+            const onAbort = () => {
+                closeSelector();
+                reject(new Error("Login cancelled"));
+            };
+            const selector = new ExtensionSelectorComponent(prompt.message, labels, (optionLabel) => {
+                closeSelector();
+                const id = prompt.options.find((option) => option.label === optionLabel)?.id;
+                if (id)
+                    resolve(id);
+                else
+                    reject(new Error("Login cancelled"));
+            }, () => {
+                closeSelector();
+                reject(new Error("Login cancelled"));
+            });
+            prompt.signal?.addEventListener("abort", onAbort, { once: true });
+            dialog[LOGIN_PROMPT_CANCEL] = onAbort;
+            handle = this.showComponentOverlay(selector, selector, "auth");
         });
     }
     async showAuthPrompt(dialog, prompt) {
+        if (prompt.signal?.aborted)
+            throw new Error("Login cancelled");
         let response;
         if (prompt.type === "select") {
             response = this.showAuthSelect(dialog, prompt);
         }
         else if (prompt.type === "manual_code") {
             response = dialog.showManualInput(prompt.message);
         }
         else {
             response = dialog.showPrompt(prompt.message, prompt.placeholder);
         }
         if (!prompt.signal)
             return response;
         if (prompt.signal.aborted)
             throw new Error("Login cancelled");
         const signal = prompt.signal;
         let onAbort;
         const aborted = new Promise((_resolve, reject) => {
             onAbort = () => reject(new Error("Login cancelled"));
             signal.addEventListener("abort", onAbort, { once: true });
         });
         try {
             return await Promise.race([response, aborted]);
         }
         finally {
             if (onAbort)
                 signal.removeEventListener("abort", onAbort);
         }
     }
     notifyAuthDialog(dialog, event) {
         if (event.type === "auth_url") {
             dialog.showAuth(event.url, event.instructions);
         }
         else if (event.type === "device_code") {
             dialog.showDeviceCode(event);
             dialog.showWaiting("Waiting for authentication...");
         }
         else if (event.type === "info") {
             dialog.showInfo(event.message, event.links);
         }
         else {
             dialog.showProgress(event.message);
         }
     }
     async loginProvider(dialog, providerId, method) {
         await this.session.modelRuntime.login(providerId, method, {
             signal: dialog.signal,
             prompt: (prompt) => this.showAuthPrompt(dialog, prompt),
             notify: (event) => this.notifyAuthDialog(dialog, event),
         });
     }
     async showLoginDialog(providerId, providerName) {
         const previousModel = this.session.model;
         const dialog = new LoginDialogComponent(this.ui, providerId, (_success, _message) => { }, providerName);
-        this.editorContainer.clear();
-        this.editorContainer.addChild(dialog);
-        this.ui.setFocus(dialog);
-        this.ui.requestRender();
-        const restoreEditor = () => {
-            this.editorContainer.clear();
-            this.editorContainer.addChild(this.editor);
-            this.ui.setFocus(this.editor);
-            this.ui.requestRender();
-        };
+        const restoreEditor = this.showLoginDialogModal(dialog);
         try {
             await this.loginProvider(dialog, providerId, "oauth");
             restoreEditor();
             await this.completeProviderAuthentication(providerId, providerName, "oauth", previousModel);
         }
         catch (error) {
             restoreEditor();
             const errorMsg = error instanceof Error ? error.message : String(error);
             if (error instanceof CredentialSynchronizationError) {
                 this.showError(`Logged in to ${providerName}, but local model state could not be synchronized: ${errorMsg}`);
