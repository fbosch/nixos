diff --git a/dist/core/agent-session.d.ts b/dist/core/agent-session.d.ts
index 61a6edb..063ff0f 100644
--- a/dist/core/agent-session.d.ts
+++ b/dist/core/agent-session.d.ts
@@ -243,6 +243,9 @@ export declare class AgentSession {
     private _runningToolCalls;
     private _nestedToolCallIds;
     private _baseSystemPrompt;
+    private _startupSessionId;
+    private _startupOwnerRevision;
+    private _startupSnapshot;
     private _baseSystemPromptOptions;
     private _systemPromptOverride?;
     constructor(config: AgentSessionConfig);
@@ -536,6 +539,7 @@ export declare class AgentSession {
     get autoCompactionEnabled(): boolean;
     bindExtensions(bindings: ExtensionBindings): Promise<void>;
     private extendResourcesFromExtensions;
+    private _publishStartupSnapshot;
     private buildExtensionResourcePaths;
     private getExtensionSourceLabel;
     private _applyExtensionBindings;
diff --git a/dist/core/agent-session.js b/dist/core/agent-session.js
index bd90fc4..b4a68f1 100644
--- a/dist/core/agent-session.js
+++ b/dist/core/agent-session.js
@@ -12,6 +12,7 @@
  *
  * Modes use this class and add their own I/O layer on top.
  */
+import { randomUUID } from "node:crypto";
 import { readFileSync } from "node:fs";
 import { basename, dirname } from "node:path";
 import { contentText, uuidv7 } from "@earendil-works/pi-ai";
@@ -70,6 +71,64 @@ function estimateMessagesTokens(messages) {
     }
     return tokens;
 }
+function freezeSnapshot(value) {
+    if (value && typeof value === "object" && !Object.isFrozen(value)) {
+        for (const child of Object.values(value))
+            freezeSnapshot(child);
+        Object.freeze(value);
+    }
+    return value;
+}
+function snapshotCopy(snapshot) {
+    return freezeSnapshot(structuredClone(snapshot));
+}
+function createStartupSnapshot(sessionId, generationId, initialRevision) {
+    let active = true;
+    let ownerRevision = initialRevision;
+    let resources = { status: "collecting" };
+    let context = { status: "collecting" };
+    const listeners = new Set();
+    const current = () => snapshotCopy({ sessionId, generationId, ownerId: "pi-runtime", ownerRevision, resources, context });
+    const assertActive = () => {
+        if (!active)
+            throw new Error("Startup snapshot runtime is stale after session replacement or reload.");
+    };
+    const notify = () => {
+        const snapshot = current();
+        for (const listener of listeners)
+            listener(snapshot);
+    };
+    const api = freezeSnapshot({
+        capability: "pi.startupSnapshot",
+        schemaVersion: 1,
+        get() {
+            assertActive();
+            return current();
+        },
+        subscribe(listener) {
+            assertActive();
+            listeners.add(listener);
+            return () => listeners.delete(listener);
+        },
+    });
+    return {
+        api,
+        publish(next) {
+            assertActive();
+            ownerRevision += 1;
+            resources = next.resources ?? resources;
+            context = next.context ?? context;
+            notify();
+        },
+        invalidate() {
+            active = false;
+            listeners.clear();
+        },
+    };
+}
+function estimateTextTokens(text) {
+    return Math.ceil(text.length / 4);
+}
 function sourceInfoMatches(left, right) {
     return left.path === right.path &&
         left.source === right.source &&
@@ -145,6 +204,9 @@ export class AgentSession {
     _nestedToolCallIds = new Set();
     // Base system prompt (without extension appends) - used to apply fresh appends each turn
     _baseSystemPrompt = "";
+    _startupSessionId = randomUUID();
+    _startupOwnerRevision = 0;
+    _startupSnapshot;
     _baseSystemPromptOptions;
     _systemPromptOverride;
     constructor(config) {
@@ -665,6 +727,7 @@ export class AgentSession {
    * Call this when completely done with the session.
    */
     dispose() {
+        this._startupSnapshot?.invalidate();
         try {
             this.abortRetry();
             this.abortCompaction();
@@ -2039,6 +2102,7 @@ export class AgentSession {
         this._applyExtensionBindings(this._extensionRunner);
         await this._extensionRunner.emit(this._sessionStartEvent);
         await this.extendResourcesFromExtensions(this._sessionStartEvent.reason === "reload" ? "reload" : "startup");
+        this._publishStartupSnapshot();
     }
     async extendResourcesFromExtensions(reason) {
         if (!this._extensionRunner.hasHandlers("resources_discover")) {
@@ -2057,6 +2121,82 @@ export class AgentSession {
         this._baseSystemPrompt = this._rebuildSystemPrompt(this.getActiveToolNames());
         this.agent.state.systemPrompt = this._baseSystemPrompt;
     }
+    _publishStartupSnapshot() {
+        const snapshot = this._startupSnapshot;
+        if (!snapshot)
+            return;
+        const resourceAggregate = this._resourceLoader.getStartupExtensionAggregate?.();
+        const skillEntries = new Map();
+        for (const skill of this._resourceLoader.getSkills().skills)
+            skillEntries.set(skill.filePath, skill);
+        const contextWindowTokens = this.model?.contextWindow;
+        const compactionEnabled = this.settingsManager.getCompactionEnabled();
+        const autoCompactReserveTokens = compactionEnabled ? this.settingsManager.getCompactionReserveTokens() : 0;
+        let context = { status: "unavailable" };
+        if (Number.isFinite(contextWindowTokens) && contextWindowTokens > 0 &&
+            Number.isFinite(autoCompactReserveTokens) && autoCompactReserveTokens >= 0 && this._baseSystemPromptOptions) {
+            const options = this._baseSystemPromptOptions;
+            const fullPrompt = this._baseSystemPrompt;
+            const withoutContextFiles = buildSystemPrompt({ ...options, contextFiles: [] });
+            const withoutContextFilesOrSkills = buildSystemPrompt({ ...options, contextFiles: [], skills: [] });
+            const withoutContextFilesSkillsOrTools = buildSystemPrompt({ ...options, contextFiles: [], skills: [], toolSnippets: {}, promptGuidelines: [] });
+            const categoryChars = new Map();
+            const addChars = (id, chars) => {
+                if (chars > 0)
+                    categoryChars.set(id, (categoryChars.get(id) ?? 0) + chars);
+            };
+            addChars("context-files", Math.max(0, fullPrompt.length - withoutContextFiles.length));
+            addChars("skills", Math.max(0, withoutContextFiles.length - withoutContextFilesOrSkills.length));
+            let promptWithoutTools = withoutContextFilesSkillsOrTools;
+            let toolOptions = { ...options, contextFiles: [], skills: [], toolSnippets: {}, promptGuidelines: [] };
+            for (const name of options.selectedTools ?? []) {
+                const entry = this._toolDefinitions.get(name);
+                if (!entry)
+                    continue;
+                const snippet = options.toolSnippets?.[name];
+                const guidelines = (entry.definition.promptGuidelines ?? []).map((guideline) => guideline.trim()).filter(Boolean);
+                const nextPrompt = buildSystemPrompt({
+                    ...toolOptions,
+                    toolSnippets: snippet ? { ...toolOptions.toolSnippets, [name]: snippet } : toolOptions.toolSnippets,
+                    promptGuidelines: [...toolOptions.promptGuidelines, ...guidelines],
+                });
+                const source = `${entry.sourceInfo.source ?? ""} ${entry.sourceInfo.id ?? ""} ${entry.sourceInfo.label ?? ""}`;
+                const category = entry.sourceInfo.source === "builtin" ? "system-tools" : /(^|[^a-z])mcp([^a-z]|$)/i.test(source) ? "mcp-tools" : "custom-tools";
+                addChars(category, Math.max(0, nextPrompt.length - promptWithoutTools.length));
+                addChars(category, JSON.stringify({ name: entry.definition.name, description: entry.definition.description, parameters: entry.definition.parameters }).length);
+                toolOptions = {
+                    ...toolOptions,
+                    toolSnippets: snippet ? { ...toolOptions.toolSnippets, [name]: snippet } : toolOptions.toolSnippets,
+                    promptGuidelines: [...toolOptions.promptGuidelines, ...guidelines],
+                };
+                promptWithoutTools = nextPrompt;
+            }
+            addChars("system-prompt", withoutContextFilesSkillsOrTools.length);
+            const valueCategories = Array.from(categoryChars, ([id, chars]) => ({ id, tokens: estimateTextTokens(" ".repeat(chars)) }));
+            context = {
+                status: "ready",
+                value: {
+                    contextWindowTokens,
+                    autoCompactReserveTokens,
+                    estimatedTokens: valueCategories.reduce((total, category) => total + category.tokens, 0),
+                    categories: valueCategories,
+                },
+            };
+        }
+        snapshot.publish({
+            resources: resourceAggregate ? {
+                status: "ready",
+                value: {
+                    extensions: resourceAggregate,
+                    skills: {
+                        available: skillEntries.size,
+                        project: Array.from(skillEntries.values()).filter((entry) => entry.sourceInfo.scope === "project").length,
+                    },
+                },
+            } : { status: "unavailable" },
+            context,
+        });
+    }
     buildExtensionResourcePaths(entries) {
         return entries.map((entry) => {
             const source = this.getExtensionSourceLabel(entry.extensionPath);
@@ -2363,7 +2503,10 @@ export class AgentSession {
                 bash: { commandPrefix: shellCommandPrefix, shellPath },
             });
         this._baseToolDefinitions = new Map(Object.entries(baseToolDefinitions).map(([name, tool]) => [name, tool]));
+        this._startupSnapshot?.invalidate();
+        this._startupSnapshot = createStartupSnapshot(this._startupSessionId, randomUUID(), ++this._startupOwnerRevision);
         const extensionsResult = this._resourceLoader.getExtensions();
+        extensionsResult.runtime.startupSnapshot = this._startupSnapshot.api;
         if (options.flagValues) {
             for (const [name, value] of options.flagValues) {
                 extensionsResult.runtime.flagValues.set(name, value);
@@ -2392,6 +2535,7 @@ export class AgentSession {
         const previousFlagValues = oldRunner.getFlagValues();
         await emitSessionShutdownEvent(oldRunner, { type: "session_shutdown", reason: "reload" });
         oldRunner.invalidate();
+        this._startupSnapshot?.invalidate();
         await this.settingsManager.reload();
         this.syncQueueModesFromSettings();
         resetApiProviders();
@@ -2409,6 +2553,7 @@ export class AgentSession {
             await options?.beforeSessionStart?.();
             await this._extensionRunner.emit({ type: "session_start", reason: "reload" });
             await this.extendResourcesFromExtensions("reload");
+            this._publishStartupSnapshot();
         }
     }
     // =========================================================================
diff --git a/dist/core/resource-loader.d.ts b/dist/core/resource-loader.d.ts
index 0a908d3..1fde48c 100644
--- a/dist/core/resource-loader.d.ts
+++ b/dist/core/resource-loader.d.ts
@@ -28,6 +28,11 @@ export interface ResourceLoaderReloadOptions {
 }
 export interface ResourceLoader {
     getExtensions(): LoadExtensionsResult;
+    getStartupExtensionAggregate?(): {
+        enabled: number;
+        project: number;
+        loadFailed: number;
+    };
     getSkills(): {
         skills: Skill[];
         diagnostics: ResourceDiagnostic[];
@@ -162,6 +167,7 @@ export declare class DefaultResourceLoader implements ResourceLoader {
     private lastPromptPaths;
     private lastThemePaths;
     private loaded;
+    private startupExtensionAggregate;
     constructor(options: DefaultResourceLoaderOptions);
     getExtensions(): LoadExtensionsResult;
     getSkills(): {
diff --git a/dist/core/resource-loader.js b/dist/core/resource-loader.js
index c5ed4d7..403dc40 100644
--- a/dist/core/resource-loader.js
+++ b/dist/core/resource-loader.js
@@ -152,6 +152,7 @@ export class DefaultResourceLoader {
     lastPromptPaths;
     lastThemePaths;
     loaded;
+    startupExtensionAggregate = { identities: [] };
     constructor(options) {
         this.cwd = resolvePath(options.cwd);
         this.agentDir = resolvePath(options.agentDir);
@@ -203,6 +204,15 @@ export class DefaultResourceLoader {
     getExtensions() {
         return this.extensionsResult;
     }
+    getStartupExtensionAggregate() {
+        const loaded = new Set(this.extensionsResult.extensions.map((extension) => extension.resolvedPath));
+        const identities = new Map(this.startupExtensionAggregate.identities.map((identity) => [identity.path, identity]));
+        return Array.from(identities.values()).reduce((aggregate, identity) => ({
+            enabled: aggregate.enabled + 1,
+            project: aggregate.project + (identity.scope === "project" ? 1 : 0),
+            loadFailed: aggregate.loadFailed + (loaded.has(identity.path) ? 0 : 1),
+        }), { enabled: 0, project: 0, loadFailed: 0 });
+    }
     getSkills() {
         return { skills: this.skills, diagnostics: this.skillDiagnostics };
     }
@@ -316,6 +325,12 @@ export class DefaultResourceLoader {
         const extensionPaths = this.noExtensions
             ? cliEnabledExtensions
             : this.mergePaths(cliEnabledExtensions, enabledExtensions);
+        this.startupExtensionAggregate = {
+            identities: [
+                ...extensionPaths.map((path) => ({ path: this.resolveExtensionLoadPath(path), scope: metadataByPath.get(path)?.scope ?? metadataByPath.get(this.resolveExtensionLoadPath(path))?.scope })),
+                ...this.extensionFactories.map((input, index) => ({ path: `<inline:${typeof input === "function" ? index + 1 : input.name}>`, scope: "temporary" })),
+            ],
+        };
         const extensionsResult = await this.loadFinalExtensionSet(extensionPaths, preTrustExtensions);
         for (const p of this.additionalExtensionPaths) {
             if (isLocalPath(p)) {
diff --git a/dist/core/extensions/loader.js b/dist/core/extensions/loader.js
index ad023c7..a69cab6 100644
--- a/dist/core/extensions/loader.js
+++ b/dist/core/extensions/loader.js
@@ -256,6 +256,10 @@ function createExtensionAPI(extension, runtime, cwd, eventBus) {
         loadingUnsubscribers.length = 0;
     };
     const api = {
+        get startupSnapshot() {
+            assertActive();
+            return runtime.startupSnapshot;
+        },
         // Registration methods - write to extension
         on(event, handler) {
             assertActive();
diff --git a/dist/core/extensions/types.d.ts b/dist/core/extensions/types.d.ts
index cbf3e0d..b5571eb 100644
--- a/dist/core/extensions/types.d.ts
+++ b/dist/core/extensions/types.d.ts
@@ -929,7 +929,34 @@ export interface  OpenAICapabilityRegistration {
   readonly asyncTools: boolean;
   readonly asyncToolNames?: readonly string[];
 }
+export type StartupSnapshotValue<T> = { readonly status: "collecting" | "unavailable"; } | { readonly status: "ready"; readonly value: T; };
+export interface StartupResourceCounts {
+    readonly extensions: { readonly enabled: number; readonly project: number; readonly loadFailed: number; };
+    readonly skills: { readonly available: number; readonly project: number; };
+}
+export type StartupContextCategory = "system-prompt" | "system-tools" | "custom-tools" | "mcp-tools" | "context-files" | "skills";
+export interface StartupContextEstimate {
+    readonly contextWindowTokens: number;
+    readonly autoCompactReserveTokens: number;
+    readonly estimatedTokens: number;
+    readonly categories: readonly { readonly id: StartupContextCategory; readonly tokens: number; }[];
+}
+export interface StartupRuntimeSnapshot {
+    readonly sessionId: string;
+    readonly generationId: string;
+    readonly ownerId: "pi-runtime";
+    readonly ownerRevision: number;
+    readonly resources: StartupSnapshotValue<StartupResourceCounts>;
+    readonly context: StartupSnapshotValue<StartupContextEstimate>;
+}
+export interface StartupSnapshotAPI {
+    readonly capability: "pi.startupSnapshot";
+    readonly schemaVersion: 1;
+    get(): Readonly<StartupRuntimeSnapshot>;
+    subscribe(listener: (snapshot: Readonly<StartupRuntimeSnapshot>) => void): () => void;
+}
 export interface ExtensionAPI {
+    readonly startupSnapshot?: StartupSnapshotAPI;
     
   /** Exact model opt-in. Register during loading or while idle; duplicates throw. Reload clears registrations. */
   registerOpenAICapabilities(registration: OpenAICapabilityRegistration): void;
@@ -1267,6 +1294,7 @@ export type SetLabelHandler = (entryId: string, label: string | undefined) => vo
  * Contains flag values (defaults set during registration, CLI values set after).
  */
 export interface ExtensionRuntimeState {
+    startupSnapshot?: StartupSnapshotAPI;
     
   openAICapabilities: Map<string, Readonly<OpenAICapabilityRegistration>>;
   isOpenAIRegistrationIdle: () => boolean;
